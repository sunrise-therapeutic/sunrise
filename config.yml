backend:
  name: git-gateway          # Netlify’s built‑in gateway
  branch: main               # your default branch name
  auth_type: auth0           # tells CMS to use the Auth0 plugin you installed

media_folder: "static/uploads"
public_folder: "/uploads"

# No collections yet – this will just show the CMS welcome screen